apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kuberag-api
  namespace: rag
  labels:
    app.kubernetes.io/part-of: kuberag
spec:
  parentRefs:
    - name: kuberag
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/
      timeouts:
        request: 60s
        backendRequest: 55s
      backendRefs:
        - name: kuberag-rag-api
          port: 80
