apiVersion: v1
kind: Pod
metadata:
  name: kuberag-unsafe-root
  namespace: rag
spec:
  containers:
    - name: unsafe
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: true
        runAsUser: 0
