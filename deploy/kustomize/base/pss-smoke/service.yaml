apiVersion: v1
kind: Service
metadata:
  name: kuberag-pss-smoke
  namespace: rag
  labels:
    app.kubernetes.io/name: kuberag-pss-smoke
    app.kubernetes.io/part-of: kuberag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kuberag-pss-smoke
  ports:
    - name: http
      port: 80
      targetPort: http
