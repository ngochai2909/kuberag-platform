apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: kuberag
  namespace: rag
spec:
  gatewayClassName: kuberag
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
