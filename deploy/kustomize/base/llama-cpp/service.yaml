apiVersion: v1
kind: Service
metadata:
  name: kuberag-llm
  namespace: rag
  labels:
    app.kubernetes.io/name: kuberag-llm
    app.kubernetes.io/part-of: kuberag
spec:
  selector:
    app.kubernetes.io/name: kuberag-llm
  ports:
    - name: http
      port: 8080
      targetPort: http
