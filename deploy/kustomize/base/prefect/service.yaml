apiVersion: v1
kind: Service
metadata:
  name: prefect-server
  namespace: prefect
  labels:
    app.kubernetes.io/name: prefect-server
    app.kubernetes.io/part-of: kuberag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prefect-server
  ports:
    - name: api
      port: 4200
      targetPort: api
