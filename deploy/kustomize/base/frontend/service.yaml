apiVersion: v1
kind: Service
metadata:
  name: kuberag-web
  namespace: rag
  labels:
    app.kubernetes.io/name: kuberag-web
    app.kubernetes.io/part-of: kuberag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kuberag-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
