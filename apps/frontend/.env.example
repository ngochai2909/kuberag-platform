# Keep mock mode until browser authentication is designed and approved.
VITE_API_MODE=mock
VITE_API_BASE_URL=/api/v1
# When VITE_API_MODE=real, Vite proxies /api to this gateway (see vite.config.ts).
VITE_DEV_API_PROXY_TARGET=http://136.85.35.106:8080
