# PERF-001 — k6 RAG load run

## Phạm vi và an toàn

- Script: `tests/k6/load.js` tại commit chứa `setup` hiện hành.
- Đích: Envoy public gateway (URL đã redacted); không gọi Kubernetes Service
  hay Pod trực tiếp.
- Cấu hình: ramp `1 -> 2 -> 3` VU, think time 35 giây, tối đa 3 VU.
- Output gốc: `load-summary.json` trong cùng thư mục.

## Kết quả thực tế

Snapshot summary được thu lúc `2026-08-03T08:19:16Z`.

| Metric | Kết quả | Ngưỡng | Kết luận |
| --- | ---: | ---: | --- |
| HTTP requests / iterations | 9 / 9 | — | Hoàn tất |
| HTTP failure | 0% (0/9) | <5% | Pass |
| RAG failure metric | 0% (0/9) | <5%, stop >20% | Pass; không kích hoạt stop condition |
| HTTP p95 | 2,458 ms | <55.000 ms | Pass |
| HTTP p90 / median / max | 2.187 / 1.208 / 2.730 s | — | Ghi nhận |
| Kiểm tra nội dung | 45/45 pass | `200`, answer, source, request ID, trace ID | Pass |
| Correlation IDs | 9 response | mọi response | Pass |

Tốc độ trung bình của toàn scenario là `0,0413 request/s`; đây là mức tải có
chủ đích thấp để không vượt shared Envoy limit `10 request/phút`. Đây không
phải benchmark công suất cực đại.

## Snapshot sức khỏe sau tải

- Node lúc `2026-08-03T08:19:16Z`: `378m` CPU (4%), `4.835 MiB` RAM (30%).
- Không có Pod phase khác `Running`/`Succeeded` trong truy vấn sau tải.
- Prometheus ghi nhận `sum(increase(envoy_http_local_rate_limit_rate_limited[5m])) = 0`
  trước bài burst riêng; load test không bị rate limited.

