# SEC-006 — Cosign keyless signing and verification

CI run #31 completed the step **Keylessly sign and verify immutable image
digests** with `success` for API, ingestion and frontend. The workflow verifies
the GitHub Actions OIDC issuer and the exact repository workflow identity on
`refs/heads/main`; it does not use a Cosign private key stored in GitHub.

Run: <https://github.com/ngochai2909/kuberag-platform/actions/runs/30798179207>

